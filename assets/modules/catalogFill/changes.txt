
Версия 1.3.8

1. Исправлена ошибка, из-за которой при импорте из XLS появлялись лишние слеши перед кавычками.

2. Исправлена ошибка, из-за которой при очищении каталога вместе с подкатегориями не удалялись TV товаров.

---------------------------------------------

Версия 1.3.7

- Исправлена ошибка из-за которой при экспорте иногда получалась не корректная сруктура каталога.

---------------------------------------------

Версия 1.3.6

- Исправлена ошибка из-за которой при импорте и экспорте товаров в/из каталог Shopkeeper (не документы MODX) могли пропускаться товары (если находятся не в документах-контейнерах).

---------------------------------------------

Версия 1.3.5

1. Исправлена ошибка, которая возникала при обновлении с версии 1.3.4, если не обновить конфиг. файл.

---------------------------------------------

Версия 1.3.4

1. Сделана возможность (для программистов) обработать значения перед импортом или экспортом (смотреть пример в конфигурационном файле).

2. Переработан интерфейс.

3. Мелкие доработки.

---------------------------------------------

Версия 1.3.3

1. Сделано удаление лишних пробелов в начале и конце названия товаров и категорий.

2. Добавлено удаление нежелательных символов в псевдониме при транслитерации.

3. В конфигурационные файлы для примера добавлены строки для добавления товарам дат.

---------------------------------------------

Версия 1.3.2

1. Исравлена замена буквы "а" на саму себя в транслитерации псевдонимов.

2. Теперь в импортируемом файле не обязательно наличие названия товара (pagetitle) при обновлении.  

---------------------------------------------

Версия 1.3.1

1. Сделана автоматическая генерация псевдонимов для категорий при импорте ($cf_config['imp_autoalias']).

2. Исправлены мелкие ошибки.

---------------------------------------------

Версия 1.3

1. В конфигурацию добавлен параметр $cf_config['imp_autoalias'] - автоматически генерировать псевдоним (alias) при импорте.
   false - выключено; true - генерировать с переводом в транслит; 'notranslit' - генерировать без перевода в транслит.

2. Исправлены мелкие ошибки.

---------------------------------------------

Версия 1.2

1. Сделана поддержка неограниченной вложенности категорий товаров.

2. Для каждой категории можно назначить свой шаблон (по ID). Например:
   $cf_config['content_row'] = array(
     0 => array('Категория',array(7,'category'))
     ...
    );
    
    7 - ID шаблона для категории.

---------------------------------------------

Версия 1.1

1. В конфигурацию добавлены параметры:

$cf_config['imp_chk_field'] - по какому полю проверять соответствие товара при обновлении. false - не проверять (очистка категории при обновлении).

$cf_config['imp_chk_tvid_val'] - проверять соответствие товара при обновлении по значению TV. Указать ID TV. false - не проверять (очистка категории при обновлении).

---------------------------------------------

Версия 1.0.2

1. Исправлена ошибка с очисткой каталога при $cf_config['include_categories'] = true; 

2. Добавлен параметр $cf_config['delete_subcategories'] для удаления дочерних категорий при очистке и обновлении каталога.

---------------------------------------------

Планируется:

1. Добавить настройку чтобы можно было товары, которых нет в файле при импорте отправить в корзину (пометить как "удален").

2. Сделать возможность обновлять только один параметр у товаров, не трогая остальные.

